import { Time } from "@angular/common";

export interface EventInfo {
    id: string,
    name: string,
    location: string,
    photo: string,
    dateOnly: string,
    hour: string,
    date: Date
}